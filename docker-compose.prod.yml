version: '3.8'
services:
  hello-world:
    image: ghcr.io/robinsondan87/robbohome-hello-world:${VERSION:-latest}
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - APP_VERSION=${VERSION:-unknown}
